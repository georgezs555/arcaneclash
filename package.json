{
  "name": "arcaneclash",
  "version": "0.1.0",
  "private": true,
  "description": "A browser card game with 2D animations and (soon) multiplayer. Hearthstone-style mechanics, original content.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @arcaneclash/client run dev",
    "server": "npm --workspace @arcaneclash/server run dev",
    "smoke": "npm --workspace @arcaneclash/server run smoke",
    "build": "npm --workspace @arcaneclash/client run build",
    "preview": "npm --workspace @arcaneclash/client run preview",
    "test": "npm --workspace @arcaneclash/engine run test",
    "typecheck": "npm --workspace @arcaneclash/engine run typecheck && npm --workspace @arcaneclash/client run typecheck && npm --workspace @arcaneclash/server run typecheck"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
